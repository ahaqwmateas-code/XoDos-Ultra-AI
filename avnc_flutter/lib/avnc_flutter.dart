library avnc_flutter;

// Placeholder - replace with actual VNC implementation
class AvncClient {
  Future<void> connect(String host, int port) async {
    throw UnimplementedError('VNC not implemented in this build');
  }
}
